'''
Question

Given two sorted arrays, find and print the elements that are common in both arrays.
Each element in the result should appear only once, even if it appears multiple times in both arrays.

Example:
Input:
arr1 = {1, 2, 2, 3, 4}
arr2 = {2, 2, 3, 5}
Output:
Intersection: 2 3

'''


#include <stdio.h>

int main() {
    int arr1[] = {1, 2, 2, 3, 4};
    int arr2[] = {2, 2, 3, 5};
    int n1 = sizeof(arr1) / sizeof(arr1[0]);
    int n2 = sizeof(arr2) / sizeof(arr2[0]);
    int i = 0, j = 0;

    printf("Intersection: ");
    while (i < n1 && j < n2) {
        if (arr1[i] == arr2[j]) {
            if (i == 0 || arr1[i] != arr1[i - 1])
                printf("%d ", arr1[i]);
            i++; j++;
        } 
        else if (arr1[i] < arr2[j]) i++;
        else j++;
    }
    return 0;
}
