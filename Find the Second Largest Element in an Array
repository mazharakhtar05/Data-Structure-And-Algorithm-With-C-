'''
Question

Write a program to find the second largest element in an array without using any library functions.

Example:
Input:
arr = {12, 35, 1, 10, 34, 1}
Output:
Second Largest Element: 34

'''

#include <stdio.h>

int main() {
    int arr[] = {12, 35, 1, 10, 34, 1};
    int n = sizeof(arr) / sizeof(arr[0]);
    int first = -1, second = -1;
    for (int i = 0; i < n; i++) {
        if (arr[i] > first) {
            second = first;
            first = arr[i];
        } else if (arr[i] > second && arr[i] != first) {
            second = arr[i];
        }
    }
    if (second == -1)
        printf("No second largest element");
    else
        printf("Second Largest Element: %d", second);
    return 0;
}
